import { StyledFooter } from '../styledComponents/styledComponents';

const Footer = () => {
    return (
        <StyledFooter>
            Footer
        </StyledFooter>
    );
};

export default Footer;